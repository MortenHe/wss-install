=== Audio / Video Player ===
- Raspberry Pi Imager -> Raspian with Desktop (ohne recommended Software)

(- USB Stick mit NTFS formattieren als "usb_audio" (Inhalte kommen später per Nextcloud))

- HDMI Kabel und USB Tastatur / Maus einstecken bei Pi und starten

(- Login pi / raspberry -> Achtung mit z)

- Config durchklicken

- Bluetooth deaktivieren

- In Raspi-Config: SSH aktivieren

sudo -s
git clone https://github.com/MortenHe/wss-install
cd wss-install
cp config-audio.dist config (Achtung nicht config-audio) | cp config-video.dist config (Achtung nicht config-video)
thonny config (PW setzen)
./install-audio-player.sh | ./install-video-player.sh

=== statische IP-Vergabe ===

service dhcpcd status

//Sollte dies nicht der Fall sein, aktivieren Sie den DHCPC wie folgt:
service dhcpcd start
systemctl enable dhcpcd

thonny /etc/dhcpcd.conf

interface wlan0
static ip_address=192.168.0.149/24
static routers=192.168.0.1
static domain_name_servers=192.168.0.1