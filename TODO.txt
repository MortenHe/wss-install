# USB
-- rsync. Sync der Hörspiele (Nextcloud)

# AudioClient
-- kein Ping, Variable connected
-- Tracks splitten (Bibi und Tina)
-- CDs für Audio Player (Weihnachten, Uhubande)
-- bei connect vorherigen (pending) canceln vs. ping rausnehmen
-- multi deploy (auf alle 3 Player aufspielen)
Kinder HSP und Musik prüfen für Brennen und Player (Nola, Piratenschwein)
htaccess / linkseite für 150/
JSON von zentraler Stelle laden?
Deploy Skripte: ssh.close();
Favicon mit besserer Auflösung und rund und pro Instanz
toggle paused mit null statt ""
color über environment (grün 149, rosa Laila)
volume Wert anzeigen in Oberfläche, bei kleiner Auflösung ggf. ausblenden
display:flex über :host in die Komponente ziehen aus search.html
Resultlist.ts, etc. prüfen welche Abos benötigt
Playlist-View per Route ansteuern
Filter, Suchterm etc. per URL-Parameter setzen
Zeit erster Track (20 min statt 3 min)

# AudioServer
path / mode / lastSession überdenken
execSync vs. exec
for (let) vs. for (const)
Mockserver
Colors für console.log

# NewSHAudioClient
Audiomodes für Laila und Luis
color-grey (z.B. connection icon) uber body setzen
searchfield und searchresult aufteilen
getFiles nur einmal aufrufen bei searchfield. Achtung bei Mode-Wechsel
aktuelles Volume nur aus Server laden (kein Startwert 50)
Ripple / Fade bei Enqeue / Remove (setTimeout?) vs. Spinner
config pw vs. marlen
Trackby für Trefferliste
Nur Reihenfolge der Tracks von Server erhalten, Anzeige der Titel über Array

# NewSHAudioServer
Songs in Drive speichern und passend benennen
mock-server weitere Funktionen (pause stoppt fakeCountdown)

# VideoClient
---- createVideoJSON ohne .mp4 bei "name" Erstellung und Großekleinschreibung wie bei Audio Json Die Maus - Maus Eis Schrift
font-awesome 5
Resultlist.ts, etc. prüfen welche Abos benötigt
Liste nach Nummer erstellen (welche Folgen von Bibi und Tina fehlen noch)

# VideoServer
Countdown umarbeiten wie bei Audio Server
Abschlussbild als Video danach random
mock-server weitere Funktionen

# WSGpioButtons
Play sound bei Click (mix mplayer)
Buttons über Config (for button of buttonConfig)

# WSRIF
IR / USB Remote Control
Karte WLAN deaktiveren

# SoundQuizGPIO

# SoundQuizServer
Spiel: "wer hat am 18.11. Geburtstag"
-- Wer spricht so: Bibi und Tina, Janoschfiguren, Woody
sendclientinfo fct
if / else vs. return

# wss-install
---- sed HifiBerry über Config
-- ls -l /dev/disk/by-uuid/ -> UUID ermittlen
-- fstab extrahieren und per sed schreiben
-- config_7070.json.dist (bei Buttons und bei cp in install script)
-- config_cards_7070.json.dist (bei RFID und bei cp in install script)
Ausgabe von install-script in Datei
shutdown sound, wenn über Button abgeschaltet
gemeinsame Tasks von Audio und Video in gemeinsames Bash auslagern und nur spezielle Anweisungen
WLAN power_off deaktiveren?
Webseite auf Server bauen und deployen?
mögliche Fehler: ^M, wenn Linux-Datei mit Windows bearbeitet wurde
git status über mehrere Projekte ermittlen (was muss auf Server commitet werden)
USB-Kabel Maus / Tastatur wegen WLAN?

--- /boot/config.txt
# Disable the rainbow splash screen
disable_splash=1

# Disable bluetooth
dtoverlay=pi3-disable-bt

# Overclock the SD Card from 50 to 100MHz
# This can only be done with at least a UHS Class 1 card
dtoverlay=sdtweak,overclock_50=100
 
# Set the bootloader delay to 0 seconds. The default is 1s if not specified.
boot_delay=0

# Overclock the raspberry pi. This voids its warranty. Make sure you have a good power supply.
force_turbo=1


Service audioplayer.service anlegen in

/etc/systemd/system

[Unit]
Description=Audio Player

[Service]
ExecStart=/usr/bin/node /home/pi/mh_prog/AudioServer/server.js > /home/pi/mh_prog/output-server.txt

[Install]
WantedBy=sysinit.target


[Unit]
Description=qtdemo
DefaultDependencies=no
[Service]
ExecStart=/usr/bin/qtdemoE -qws
[Install]
WantedBy=basic.target


sudo chmod 644 /lib/systemd/system/audioplayer.service

in Autostart mit
systemctl enable audioplayer.service


test:

sudo systemctl disable raspi-config.service
sudo systemctl disable keyboard-setup.service
sudo systemctl disable dphys-swapfile.service
sudo systemctl disable avahi-daemon.service
sudo systemctl disable triggerhappy.service


TODO
dhcpcd.service
ssh.service

No
sudo systemctl disable networking.service

wpa_supplicant.service?
apache


###

Raspian Lite

pi / raspberry (Achtung mit z)
sudo raspi-config
8 update tool
1 change user pw : martin12
2-2 Wifi Country DE -> Haustralien
3-2 Don't wait for network
3-3 ?
4-1 de utf-8
4-2 Berlin
4-3 105 - other - german - german
4-4 DE
5-2 ssh
7-1

sudo -s -> root
apt-get install git
git clone https://github.com/MortenHe
cd wss-install
cp config.dist config