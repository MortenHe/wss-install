USB
-- Sync der Hörspiele (Nextcloud)

AudioClient
-- bei connect vorheriger (pending) canceln
-- wenn keine lastSeesion zufällige Playlist?
-- Audiolist für Herbst, Ostern, etc. active = false
-- deploy mit Dateien aus done Ornder -> Ziel ermitteln wohin Datei kopiert werden soll
-- multi deploy (auf alle 3 Player aufspielen)
-- CDs für Audio Player
- htaccess / linkseite für 150/
- JSON von zentraler Stelle laden?
- Favicon mit besserer Auflösung und rund und pro Instanz
- toggle paused mit null statt ""
- color über environment (grün 149, rosa Laila)
- volume Wert anzeigen in Oberfläche, bei kleiner Auflösung ggf. ausblenden
- display:flex über :host in die Komponente ziehen aus search.html
- Resultlist.ts, etc. prüfen welche Abos benötigt
- Playlist-View per Route ansteuern
- Filter, Suchterm etc. per URL-Parameter setzen
- Zeit erster Track (20 min statt 3 min)

AudioServer
- execSync vs. exec
- for (let) vs. for (const)
- Mockserver
- Colors für console.log

NewSHAudioClient
--- Audiomodes für Laila und Luis
- color-grey (z.B. connection icon) uber body setzen
- searchfield und searchresult aufteilen
- getFiles nur einmal aufrufen bei searchfield. Achtung bei Mode-Wechsel
- aktuelles Volume nur aus Server laden (kein Startwert 50)
- Ripple / Fade bei Enqeue / Remove (setTimeout?) vs. Spinner
- config pw vs. marlen
- Trackby für Trefferliste
- Nur Reihenfolge der Tracks von Server erhalten, Anzeige der Titel über Array

NewSHAudioServer
- mock-server weitere Funktionen (pause stoppt fakeCountdown)

VideoClient
- font-awesome 5
- Resultlist.ts, etc. prüfen welche Abos benötigt
- Liste nach Nummer erstellen (welche Folgen von Bibi und Tina fehlen noch)

VideoServer
- Countdown umarbeiten wie bei Audio Server
- Abschlussbild als Video danach random
- mock-server weitere Funktionen

WSGpioButtons
--- Buttons für Lernspiel
--- Sound abspielen, wenn Button gedrückt

WSRIF
--- Karten immer spielen können (Emil Grünbär, wenn gerade in SH Audio Player)
--- Hörspielkarten über JSON-Assets steuern?
- IR / USB Remote Control
- Karte WLAN deaktiveren

SoundQuizGPIO

SoundQuizServer
--- Buttons für Joker, Next, Jingle
- Countdown
- sendclientinfo fct
- if / else vs. return

wss-install
-- boot to desktop
-- lastSession.json besserer Check
-- after-install.txt mit Befehlen zu config (mousepad statt vi)
-- Raspian with desktop -> Etcher
-- Raspi-Pop-Config durchklicken, Raspi-Config (SSH) durchklicken, Bluetooth deaktivieren
-- sudo -s, git clone wss-instll, cp config.dist config, mousepad config, ./install-audio-player.sh
- Ausgabe von install-script in Datei
-- sudo pcmanfm (Explorer als Root)
-- onoff mit sudo npm install?
-- Buttons 15, 5, 7 -> Photoshop
-- Tinkerbutton 160,253,252
-- Lernspiel install
- zusätzlich echo nach Überschrift?
-- autoconfig bei mausberry Audioserver?
--- hifiberry https://support.hifiberry.com/hc/en-us/community/posts/207306565-Raspberry-Pi-3-DAC-Step-by-Step
--- boot/config anpassen + reboot hifiberry
-- ls -l /dev/disk/by-uuid/ -> UUID ermittlen
- fstab extrahieren und per sed schreiben
-- wiring Info in extra Datei
- gemeinsame Tasks von Audio und Video in gemeinsames Bash auslagern und nur spezielle Anweisungen
- WLAN power_off deaktiveren?
- Webseite auf Server bauen und deployen?
- mögliche Fehler: ^M, wenn Linux-Datei mit Windows bearbeitet wurde
- audioOutpt Digital|PCM|Master|Headphone,1
- git status über mehrere Projekte ermittlen (was muss auf Server commitet werden)
- USB-Kabel Maus / Tastatur wegen WLAN?

git clone https://github.com/MortenHe/SoundQuizServer
npm install
cp config.json.dist config.json
--- startnodesh und startnodesound in rc.local startnode zu startnodesound.sh umbenennen
- Tinkerboard per USB verbinden und per Etcher flashen
--- wss-install config.dist raspi default true rue
----- stopnode ???
----- Laila Player NewSHAudioServer neu auschekcne wegen shutdown