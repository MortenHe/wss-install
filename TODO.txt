# AudioClient
statische IP-Vergabe

sudo service dhcpcd status

Sollte dies nicht der Fall sein, aktivieren Sie den DHCPC wie folgt:
sudo service dhcpcd start
sudo systemctl enable dhcpcd

sudo nano /etc/dhcpcd.conf

interface wlan0
static ip_address=192.168.0.149/24
static routers=192.168.0.1
static domain_name_servers=192.168.0.1

- node ws auf 7 hochziehen
- Creationdate bei Audio Video Sortierung 
- Mix-Suche mit Paging
- Nextcloud /raspi/files/wvp -> video umbenennen, danach mit Nutzer Video (existiert schon) teilen
- Logging Ausgabe > output-server.txt direkt in server.js einbauen, ggf. startnode loswerden / vereinfachen?
- Playlists / Videos ausgegraut anzeigen, wenn Datei Info schon in JSON aber Datei noch nicht von Nextcloud geladen
- reloadJSON bei Video Player nach 5 min
Arduino Sprachsteuerung
Android App
auf View Suche vs. Playlist per Route
setJoker mit Promises
kein Ping, Variable connected -> ping Zeit veroppeln?
htaccess / linkseite für 150/
Deploy Skripte: ssh.close();
Favicon mit besserer Auflösung und rund und pro Instanz
toggle paused mit null statt ""
color über environment (grün 149, rosa Laila)
volume Wert anzeigen in Oberfläche, bei kleiner Auflösung ggf. ausblenden
display:flex über :host in die Komponente ziehen aus search.html
Resultlist.ts, etc. prüfen welche Abos benötigt
Playlist-View per Route ansteuern
Filter, Suchterm etc. per URL-Parameter setzen

# AudioServer
-- WLAN abschalten
keine Eingabe mehr nach stopnode
Mpv player
execSync vs. exec
for (let) vs. for (const)

# NewSHAudioClient
-- Liste der neuesten Tracks auf der Seite zeigen
-- Underline hover bei newsh
Swipe für Titel einfügen löschen in SH Player
color-grey (z.B. connection icon) über body setzen
searchfield und searchresult aufteilen
getFiles nur einmal aufrufen bei searchfield. Achtung bei Mode-Wechsel
aktuelles Volume nur aus Server laden (kein Startwert 50)
Ripple / Fade bei Enqeue / Remove (setTimeout?) vs. Spinner
config pw vs. marlen
Trackby für Trefferliste
Nur Reihenfolge der Tracks von Server erhalten, Anzeige der Titel über Array

# NewSHAudioServer

# VideoClient
--- bereits eingefügte Folgen nicht doppelt einfügen
-- Underline hover bei video
-- VideoClient: klick auf kompletten Bereich der Zeile ermöglichen
font-awesome 5
Resultlist.ts, etc. prüfen welche Abos benötigt
Liste nach Nummer erstellen (welche Folgen von Bibi und Tina fehlen noch)

# VideoServer
--- Aktuelle Playlist als Datei auf Server, damit man bei Fehler nicht neu klicken muss
Countdown in logfile ausgeben
mock-server weitere Funktionen

# WSGpioButtons
Buttons über Config (for button of buttonConfig)

# WSRIF

# SoundQuizGPIO

# SoundQuizServer
- Answer wrong right Variationen. Quatsch, genau so ist es
Lernspiel: Wer hat am 18.11. Geburtstag, Musikinstrumente, Flaggen, Was bedeutet das frz. Wort Baguette? (Fremdsprachen), Wappen Pfaffenweiler 
sendclientinfo fct
if / else vs. return

# Gehäuse
8mm Buche
http://www.makercase.com/
https://www.festi.info/boxes.py/
https://www.formulor.de/uploadHelp

# wss-install
gemeinsame Tasks von Audio und Video in gemeinsames Bash auslagern und nur spezielle Anweisungen

mögliche Fehler: ^M, wenn Linux-Datei mit Windows bearbeitet wurde


Service audioplayer.service anlegen in

/etc/systemd/system

[Unit]
Description=Audio Player

[Service]
ExecStart=/usr/bin/node /home/pi/mh_prog/AudioServer/server.js > /home/pi/mh_prog/output-server.txt

[Install]
WantedBy=sysinit.target

[Unit]
Description=qtdemo
DefaultDependencies=no
[Service]
ExecStart=/usr/bin/qtdemoE -qws
[Install]
WantedBy=basic.target

sudo chmod 644 /lib/systemd/system/audioplayer.service

in Autostart mit
systemctl enable audioplayer.service